# ============================================
# Auth Service Environment Variables
# ============================================
# This is a TEMPLATE file showing all required environment variables.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Update the values below with your actual configuration
# 3. NEVER commit the .env file to git (it contains secrets!)
#
# Note: Values shown here are examples for DEVELOPMENT only.
# In PRODUCTION, use strong, unique values!
# ============================================

# ============================================
# Application Settings
# ============================================
APP_NAME="RideMatch Auth Service"
APP_VERSION="1.0.0"

# Set to True for development (shows detailed errors)
# Set to False for production (hides sensitive error details)
DEBUG=True

# ============================================
# Database Settings (PostgreSQL)
# ============================================
DATABASE_HOST=auth-db
DATABASE_PORT=5432
DATABASE_NAME=auth_db
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# ============================================
# Redis Settings (Cache & Sessions)
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# ============================================
# JWT (JSON Web Token) Authentication
# ============================================
JWT_SECRET_KEY=your-secret-key-CHANGE-THIS-IN-PRODUCTION
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================
CORS_ORIGINS=http://localhost:3000,http://localhost,http://127.0.0.1:3000

# ============================================
# Security Settings
# ============================================
BCRYPT_ROUNDS=12

# ============================================
# API Configuration
# ============================================
API_PREFIX=/api/auth

# ============================================
# Pagination Settings
# ============================================
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
